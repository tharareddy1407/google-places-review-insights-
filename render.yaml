services:
  - type: web
    name: google-places-review-insights
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: GOOGLE_MAPS_API_KEY
        sync: false
